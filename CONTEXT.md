# STKB 领域词汇

本词汇表统一 STKB 从资料接入、销售知识构建到多形态发布和应用使用的核心语言。它只定义领域概念，不规定数据库、文件格式或具体技术实现。

## 资料与工作区

**原始资料**：
进入 STKB、尚未经过标准化解析的来源材料。
_Avoid_: 原始知识块、原始 chunk

**知识工作区（KnowledgeWorkspace）**：
资料、正式知识和检索结构的逻辑隔离与维护边界，当前分为行业知识工作区和企业知识工作区。
_Avoid_: 数据库、租户字段、文件目录、向量 collection

**行业知识工作区**：
由平台或行业知识维护方统一维护、可单向分发给企业使用的行业知识空间。
_Avoid_: 企业私有知识库、所有企业共同修改的共享库

**企业知识工作区**：
归属于单个企业、保存其独有资料和知识资产的隔离空间，可以扩展或覆盖行业知识，但不修改行业原知识。
_Avoid_: 行业知识工作区、运行时会话空间

**资料接入与知识构建**：
覆盖资料接入、能力包解析、销售知识识别、实体归一、增量归并、正式知识文件写入、知识构建结果记录以及向量索引和知识图谱形成的完整责任链。
_Avoid_: 把整条链路简称为预处理

## 销售知识构建

**STKB 销售知识模型**：
定义什么内容属于销售训练知识、如何归入 D1—D5、怎样区分对象身份和建立关系的业务模型。
_Avoid_: AI 应用配置、提示词模板、数据库 Schema

**销售知识模型规则包**：
`STKB 销售知识模型` 在一个明确版本和生效边界内、可供识别与校验共同使用的规则表达，包含知识域、知识内容模块、对象类型和对象边界等当前有效约束。
_Avoid_: 仅含模块名称的目录、一次模型调用的 Prompt、永久不变的分类枚举

**知识域（KnowledgeDomain）**：
按销售训练中的业务职责划分知识的稳定顶层分类，当前 D1—D5 分别回答卖什么、卖给谁、怎么卖、具体怎么说和如何评价。
_Avoid_: 物理数据库、来源资料目录、知识对象类型

**知识内容模块（KnowledgeContentModule）**：
知识域下组织一组业务含义、对象身份规则、治理方式和消费路径相近的知识对象类型的治理分组。
_Avoid_: 物理子库、单项知识对象、业务实体、资料载体

**对象类型合同（ObjectType Contract）**：
`KnowledgeObject` 通过 `objectType` 选择的对象边界、身份字段、内容结构和校验规则；它是知识对象的合同属性，不是知识域与知识内容模块下面新增的目录层级。
_Avoid_: 第三级知识目录、知识内容模块、业务实体类型、任意 JSON 模板

**销售知识识别**：
当本次处理要求构建销售知识时的必经阶段，由大模型参与阅读全文并受当前销售知识模型规则约束，同时回答“哪些内容是销售知识、属于哪个域和模块、资料中提到了哪些产品／版本／客户／场景”；没有可识别销售知识时结果为零个候选知识。
_Avoid_: 可选知识分类、通用文本摘要、机械文本切分、文件级单标签分类、按五域或22个模块逐项循环调用

**业务实体归一**：
将候选知识中的实体提及解析为工作区内可稳定引用的业务身份，结果为复用已有 `BusinessEntity`、提出一个待正式写入的新实体身份，或无法确认的异常；它回答“资料里说的是谁”，不判断候选是不是同一项知识。待新增身份只有在引用它的知识处理结果被接受时，才与正式知识一起登记为 `BusinessEntity`。
_Avoid_: 销售知识归并判定、名称替换、字符串去重、知识图谱建点

**销售知识归并判定**：
在业务实体已经归一后，将候选知识与当前正式知识及本批次其他候选比较，判断忽略、补证、新增、更新、退出或异常；它回答“是不是同一项知识、这项候选应当去哪里”，不再负责确认业务实体身份。
同一份不可变资料中，唯一的同类来源位置可以作为对象连续性证据；同一位置存在多个同类对象，或本次对象身份集合发生变化时必须进入待评审，来源位置不能单独覆盖业务身份。
_Avoid_: 业务实体归一、只按模块或向量相似度去重、把每个候选直接当成正式知识

**DocumentPackage**：
一次接入的一份原始资料经能力包解析后形成的、可阅读、可定位、可复核的标准化文档证据包；它组织原件引用、按需产生的随文资源、全文 Markdown、来源定位和质量信息，以一份全文 Markdown 为解析主交付。新接入的相似或同名资料默认形成新的 `DocumentPackage`，不自动推断为原资料的新版本。
_Avoid_: 正式知识、知识构建结果、检索索引、系统自动生成的资料版本链

**随文资源（DocumentResource）**：
从原始资料中提取或为保真阅读生成、被全文 Markdown 或来源证据实际引用的非正文内容，例如图片、复杂图表、嵌入附件和必要媒体。它在全文 Markdown 组装前形成，但不是每份资料都必须产生。
_Avoid_: 原始文件、普通正文、简单表格、转写正文、OCR 或版面中间文件、解析日志、文档片段、向量 chunk

**全文 Markdown**：
原始资料完成内容提取、且必要随文资源已经可引用后组装形成的整份可读内容表达，属于 `DocumentPackage`，保持原资料的内容顺序和结构边界，是资料解析的必达业务结果。
_Avoid_: 正式知识、知识对象、知识块

**文档片段**：
从全文 Markdown 中按标题、页码、表格位置或时间段定位到的局部证据范围，可以在阅读、识别或回源时按需派生。
_Avoid_: 独立内容主存、抽取窗口业务对象、知识块、向量 chunk、正式知识对象

**候选知识对象（CandidateKnowledgeObject）**：
销售知识识别从全文 Markdown 中按业务对象边界聚合形成的对象提议，必须包含可读标题、主域、主模块、对象类型、对象边界、分类依据、身份线索、类型化内容和来源证据。它使用运行内候选 ID，尚未完成业务实体归一和跨资料归并，因此不是具有稳定身份的正式 `KnowledgeObject`。
_Avoid_: 原文自然段、机械切分结果、固定长度 chunk、抽取窗口、只有 summary 的分类标签、正式 KnowledgeObject、独立候选知识库

**知识对象（KnowledgeObject）**：
一项可以独立识别、归并、更新和复用的正式销售知识，具有稳定业务身份并关联类型化规范内容、业务实体引用、适用范围、证据、对象关系和版本；同一对象可以包含共享身份和生命周期的多个字段、条目、步骤、变体或事件。
_Avoid_: 事实原子、单个字段或表格单元格、相似文本组、Markdown 段落、向量 chunk、图节点

**知识构建结果（KnowledgeBuildResult）**：
一批 `DocumentPackage` 完成实体归一和知识归并后形成的不可变运行结果，记录本次新增、更新、复用、忽略或异常的实体、知识对象和正式关系；它没有独立业务版本或成员生命周期，也不拥有本批形成的正式知识。
_Avoid_: KnowledgeAssetPackage、正式知识集合、发布快照、运行时 KnowledgePackage

**业务实体（BusinessEntity）**：
系统已经确认“具体是谁”的产品、产品版本、客户特征、场景或阶段等业务对象。例如资料里的“药享保”“这个产品”确认指向同一产品后，系统只登记一个产品实体 ID；多项知识可以共同引用它。业务实体独立于知识图谱存在，但不保存事实、策略、话术等知识正文。
_Avoid_: KnowledgeObject、任意名词实体、图节点、纯文本标签

**实体提及（EntityMention）**：
来源原文或候选知识中出现的名称、简称或描述，例如“药享保”“这个产品”“基础版”。它只是“资料里怎么称呼”，还没有确认具体指向哪个正式业务实体。
_Avoid_: BusinessEntity、正式实体引用

**对象—实体引用（KnowledgeObjectEntityReference）**：
`KnowledgeObject` 通过业务实体 ID 关联 `BusinessEntity` 的正式记录，说明这项知识描述哪个产品、适用于哪个版本或客户、发生在哪个场景，并保留原文证据；绑定依靠 ID 和引用含义，不依靠正文里是否正好出现某个名称。
_Avoid_: content 字符串匹配、实体提及、图谱临时边

**业务图模型（BusinessGraphModel）**：
`STKB 销售知识模型` 中定义 D1—D5、知识内容模块、知识对象类型、业务实体类型、对象—实体引用、实体关系和对象关系如何对应的业务语义模型。
_Avoid_: 图数据库 Schema、图谱知识投影、通用实体抽取、按域或模块执行的模型调用拓扑

## 正式知识存储与使用

**正式知识文件**：
一个生效 `KnowledgeObject` 的规范内容表达；一个当前知识对象对应一份当前知识文件，文件中的知识身份、分类、实体引用和证据必须能够回到正式登记。
_Avoid_: 文件知识投影、全文 Markdown、向量 chunk、多个对象拼成的主题文章

**检索单元（RetrievalUnit）**：
为向量或混合检索从正式知识文件派生的可重建单元，必须回指知识对象及版本。
_Avoid_: 正式知识对象、唯一事实源

**向量检索索引**：
从正式知识文件派生的检索单元和 embedding，用于相似内容召回，不负责决定知识身份和归并结果。
_Avoid_: 正式知识集合、独立事实库

**知识图谱**：
把已确认的知识对象、业务实体、对象—实体引用、实体关系和对象关系组织成可查询图结构，用于关系扩展和影响分析；图中的节点和边回指正式登记，不反向定义对象身份。
_Avoid_: 把所有文本和表达变体都建成节点

**KnowledgePackage**：
知识使用侧针对一次请求，从当前正式知识文件、向量检索索引和知识图谱中检索、过滤、重排并组装出的运行时知识返回包。
_Avoid_: DocumentPackage、KnowledgeBuildResult、AI 应用配置
