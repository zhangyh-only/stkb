# STKB 项目协作规范

## 1. 项目定位

本仓库以方案探索为主，以代码和页面作为验证手段，不以完整系统交付为目标。

每次实现必须说明要验证的问题、输入样例、判断标准和明确不做的范围。事项不永久划定唯一负责人；实现可以重叠，但必须使用可比较的证据收敛。

## 2. 技术栈

- 后端：Python 3.12、FastAPI、Pydantic。
- 前端：Vue 3、TypeScript、Vite，统一使用 Composition API 和 `<script setup>`。
- 业务主存与运行记录：PostgreSQL 16。
- 向量检索投影：PostgreSQL pgvector。
- 图投影：Neo4j 5。
- 正式知识文件验证：本地文件系统，内容使用 Markdown。

三种知识形态是正式知识文件、向量检索投影和知识图谱投影。它们必须共享知识对象 ID 和修订标识，并可由正式登记重建。Neo4j 和 pgvector 不反向决定知识身份。

## 3. 目录约束

- `backend/app/core/`：配置、日志、健康状态等基础能力。
- `backend/app/api/`：HTTP 适配，不放领域判断。
- `backend/app/features/<capability>/`：按验证能力放业务代码。
- `frontend/src/features/<capability>/`：按验证能力放页面、状态和组件。
- `contracts/`：只放已由代码和样例验证、需要跨模块共享的合同。
- `docs/current/`：当前领域方案；不再保存历史会议转写、旧原型和过程审计。
- `workspace/`：本地运行输出，仅提交目录说明，不提交真实运行数据。
- `samples/`：经过评审、允许长期复现的脱敏输入和预期结果。

## 4. 实现约束

- 不使用魔法值。端口、连接地址、API 前缀、存储名称和跨模块标识统一从配置或常量读取。
- API 路由只做输入输出转换；方案规则进入对应 capability。
- 页面服务于能力调试，至少展示输入、中间状态、三种形态的实际数据、耗时和错误。
- 不为未来完整系统提前加入用户权限、任务中心、消息队列、缓存或微服务拆分。
- 新增存储结构时，必须说明事实源、派生关系、重建方式和一致性判断。

## 5. 验证要求

- Python：`ruff check` 和相关 `pytest`。
- Vue：TypeScript 检查和 `vite build`。
- PostgreSQL/pgvector、Neo4j 和文件输出必须使用同一真实样例联调；只验证接口 Mock 不算跑通。
- 未连接真实模型、数据库或业务环境时，结论必须明确标注边界。

## 6. Git

- 新分支默认使用 `feature/` 前缀。
- 提交信息使用 `<type>: <中文说明>`。
- 大改动按“基础设施 -> 核心逻辑 -> 接口/文档 -> 清理”拆分提交。
- 不提交 `.env`、依赖目录、构建产物和本地运行数据。
