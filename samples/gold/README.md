# 销售知识识别代理 Gold

本目录保存可提交、可评审的销售知识对象边界样本。`proxy_draft` 表示由 Codex 根据原始资料、
当前领域方案和对象合同建立的代理预期，只用于调试与回归，不等同于业务人工已批准答案。

每个 Gold 只约束当前资料中应稳定出现的重点对象组、数量、来源锚点和禁止模式；不要求为了覆盖
22 个模块而生成空对象。评审通过后才可将状态改为 `approved`。
